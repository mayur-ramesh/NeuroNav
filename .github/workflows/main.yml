name: CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f backend/requirements.txt ]; then pip install -r backend/requirements.txt; fi
        
    # Add frontend build steps here when ready
    # - name: Use Node.js
    #   uses: actions/setup-node@v3
    #   with:
    #     node-version: '18.x'
    # - name: Build frontend
    #   run: |
    #     cd frontend
    #     npm install
    #     npm run build
